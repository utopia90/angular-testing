import { StudentServiceStub } from './student-service-stub.mock';

describe('StudentServiceStub', () => {
  it('should create an instance', () => {
    expect(new StudentServiceStub()).toBeTruthy();
  });
});
